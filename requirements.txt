# --- Hardware-specific: install the matching torch wheel FIRST ---
# DGX Spark / Blackwell (CUDA 13):
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
# Other CUDA: see https://pytorch.org
torch

# Embeddings / models
transformers==4.57.6      # pinned: adapters requires transformers <5
adapters==1.3.0
sentence-transformers

# Vector store + LLM client
chromadb
openai                    # used for Ollama's OpenAI-compatible API

# Data / utils
ijson
pyyaml
tqdm
numpy
pandas

# Analysis notebook
jupyterlab
matplotlib
ipywidgets

# ColBERT (for the colbert strategy). Needs nvcc present to compile kernels.
# After install, transformers.AdamW is shimmed in rag/colbert.py (ColBERT 0.2.21
# imports it but transformers 4.x removed it; only used for training, not inference).
colbert-ai==0.2.21
faiss-cpu
